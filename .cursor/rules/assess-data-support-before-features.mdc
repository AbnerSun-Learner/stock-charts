---
description: 新功能开发前必须先评估数据库数据支持度（强制）
alwaysApply: true
---

# 新功能先评估数据支持度

涉及读共享表、画图、统计、筛选或业务判定的新功能，**在方案设计与写代码之前**必须先完成数据支持度评估；未评估不得开工实现。

## 必评维度

1. **表与字段**：目标列是否已存在、可空约束、单位/口径是否与产品语义一致
2. **覆盖范围**：有哪些 `index_code` / 标的、日期跨度、最近同步日
3. **完整率**：关键字段非空占比；多字段叠加（如 `close`∩`pe_ttm`）可对齐的样本数
4. **缺口与降级**：缺数时 UI/逻辑如何表现（空态、隐藏系列、禁用开关、提示文案）
5. **双仓边界**：缺表/缺源时只能在文档写契约，由 `scheduled-tasks` 落库后再接本仓 Repository（见 `dual-repo-data-boundary`）

## 评估方式

- 优先对真实库跑只读 SQL（覆盖计数、`min/max(trade_date)`、按标的分组）
- 对照本仓 Repository / 类型映射，确认已有读路径是否够用
- 将结论写入设计说明（spec 或对话方案）的「数据支持度」小节：结论为 **足够 / 部分足够 / 不足**

## 结论门槛

| 结论     | 动作                                                          |
| -------- | ------------------------------------------------------------- |
| 足够     | 可进入实现；注明任何已知稀疏区间                              |
| 部分足够 | 可实现，但必须定义降级与验收范围（例如仅某指数、近 N 年）     |
| 不足     | 暂停本仓 UI；先补 `scheduled-tasks` 同步/表契约，再回来做前端 |

## 禁止

- 仅凭 schema 或 mock/fixture 推断「库里一定有数」
- 在数据不足时用客户端拼凑假历史写入共享表
- 跳过评估直接做完整产品交互，事后再发现大面积空数据
