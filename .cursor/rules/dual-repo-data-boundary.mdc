---
description: stock-charts 与 scheduled-tasks 的数据职责边界（强制）
alwaysApply: true
---

# 双仓数据职责边界

本仓库（`stock-charts`）与同级 **`scheduled-tasks`** 共用同一 Supabase Postgres。职责不可混淆。

## scheduled-tasks 唯一负责

- 建表、migration、`schema.sql`、RLS 策略 SQL（含用户数据表与 `fx_rates`）
- 第三方数据源接入（yfinance、Frankfurter/ECB 等）及密钥（仅该仓 Secrets）
- 共享表批量落库（`etf_daily`、`fx_rates`、`sync_runs` 等）

## stock-charts 只负责

- 业务纯函数、UI
- 经 `@supabase/supabase-js` + Auth/RLS 的用户数据 CRUD（数据由用户在 UI 填写）
- 共享行情/汇率表的**只读**查询与领域映射
- 认证：**GitHub OAuth**（登录 Modal；家庭财务等私有功能可加允许名单）

## 禁止（本仓）

- 新增权威 DDL：`docs/supabase-schema.sql`、`supabase/migrations/*` 或等价文件
- 内嵌行情/汇率 API Key，或直连第三方拉数后写入共享表
- 客户端/应用服务对共享行情表做批量 upsert/delete

## 协作方式

需要新表或新同步源时：在本仓 `docs/superpowers/specs/` 写清表契约 → 在 `scheduled-tasks` 落地 SQL/job → 本仓再接 Repository。

共享汇率表领域映射：`FxRate.date` ↔ 物理列 `fx_rates.rate_date`。
