---
description: 家庭财务相关数据读写/操作前必须征得用户同意
alwaysApply: true
---

# 家庭财务数据同意约束

家庭财务数据属高度隐私，**任何读取或写入前必须先征得用户明确同意**。其余 Supabase 数据（行情、同步记录、网格策略等）可按现状直接查询/操作。

## 适用范围（需同意）

- 表：`family_*`、`insurance_policies`，以及任何以家庭财务为内容的视图/函数结果
- 操作：`SELECT` / `INSERT` / `UPDATE` / `DELETE`、migration、导出、在回复中展示明细金额或成员隐私字段
- 渠道：Supabase MCP（`execute_sql` 等）、应用代码联调、脚本、日志摘录

## 必须行为

1. **先问后动**：说明要查/改的表与目的，等用户明确同意后再执行
2. **最小范围**：只拉用户点名的字段与行；禁止整表倾倒或顺带扫其他家庭表
3. **回复克制**：未经同意不在对话中复述账目金额、保单细节、成员隐私信息

## 禁止

- 以「调试 / 核对 schema / 顺手看一眼」为由擅自查询家庭财务表
- 在未获同意时对家庭财务表做写入、删除、migration 或 RLS 变更
- 把家庭财务明细写入计划文档、commit message 或公开文档

## 例外（无需再问）

- 仅确认表名是否存在、行数是否为 0/非 0 的**元数据级**探测，且不返回业务字段内容——仍优先询问；若用户已在本会话明确授权某次任务，则该任务范围内可操作
- 用户说「可以查家庭财务」「导出保单」等明确授权后，按授权范围执行
